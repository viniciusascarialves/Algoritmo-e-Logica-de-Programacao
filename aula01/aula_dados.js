// Tipos de dados PRIMITIVOS
// Number, String, Boolean, Undefined, Null
let numero = 10;
let valor = 20.40; // float.

// String;
let nome = "Fulano";

// Boolean > Verdadeiro (true) ou Falso (false);
let freteGratis = true;

/*Undefined: Representa a ausência de valor. Geralmente, é o valor
 padrão para variáveis recém-declaradas.*/
let desconto;

// Null Representa a ausência intencional de qualquer valor ou objeto.
let total = null;
