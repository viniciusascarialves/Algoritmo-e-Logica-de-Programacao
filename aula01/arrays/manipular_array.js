const numeros = [1, 5, 10, 20];

/*
  push > Adiciona um item ou mais no ultimo elemento do array
  pop > Remove o ultimo elemento do array
  unshift > Adicionar elementos no inicio do array
  shift > Remover o primeiro elemento do array
*/

numeros.push(30, 50, 100);

// numeros.pop();

// numeros.unshift(500);

numeros.shift();

console.log(numeros);